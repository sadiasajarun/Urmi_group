# Google Maps JS API key for the Factories section (Section 9).
# Get one at https://console.cloud.google.com/google/maps-apis/
# Without this key, the Factories section renders a styled SVG map fallback.
VITE_GOOGLE_MAPS_API_KEY=
